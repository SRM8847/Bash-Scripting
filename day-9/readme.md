# Day 9

- Q1. Redirect the output of ls -l to a file called listing.txt.
- Q2. Redirect errors from a command (e.g., cat nonexistent.txt) to a file called error.log.
- Q3. Redirect both stdout and stderr to the same file.
- Q4. Discard the output of a command entirely (send it to /dev/null).
