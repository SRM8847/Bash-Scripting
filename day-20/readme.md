# Day 20
- Q1. Write a `for` loop that prints numbers 1 to 10.
- Q2. Write a `for` loop that iterates over a list of fruit names.
- Q3. Use a `for` loop to print all `.txt` files in the current directory.
- Q4. Use `{1..5}` brace expansion in a `for` loop.
