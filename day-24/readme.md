# Day 24
- Q1. Perform arithmetic using `$(( ))` — add two numbers.
- Q2. Calculate and print the result of `(5 * 3) - 4` using `$(( ))`.
- Q3. Use `let` to increment a counter variable in a loop.
- Q4. Use `bc` to perform floating-point division.
