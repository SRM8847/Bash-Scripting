# Day 16

- Q1. Write a script that accepts a name as the first argument and prints "Hello, <n>!".
- Q2. Print the name of the currently executing script inside the script.
- Q3. Print all arguments passed to a script using $@.
- Q4. Check if no argument was passed to a script and print a usage message.
