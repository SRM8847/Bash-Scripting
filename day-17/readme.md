# Day 17
- Q1. Use `read` to take user input and store it in a variable.
- Q2. Ask the user for their name and age, then print both.
- Q3. Use `read -s` to silently read a password (input not shown).
- Q4. Use `read` with a prompt in a single line (`read -p`).
