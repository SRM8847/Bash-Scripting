# Day 10

- Q1. Use a pipe to count the number of files listed by ls.
- Q2. Use grep to search for the word "bash" in a file (case-insensitive).
- Q3. Display lines in a file that do NOT contain the word "error".
- Q4. Chain cat, grep, and wc using pipes to count matching lines.
